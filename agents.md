# 项目协作约定

本文件适用于整个仓库。修改 Agent、提示词、工具定义或依赖时，必须遵守以下约定。

1. 要求解决问题的时候禁止只做局部修改而忽视整体的架构问题！
2. 从根本上解决问题而非从现象上解决问题
3. 所有的修改都需要能保证项目工作流符合Agentic的原则，尽量不使用固定工作流，可以使用skill

## GIT管理

当需要进行重大功能修改并有**可能损害完整可用性**的时候，提交一版GIT，避免频繁提交

## 提示词缓存与稳定前缀

- 修改任何 Agent 提示词前，先阅读 `src/agent.ts` 顶部的 `PROMPT / PREFIX-CACHE CONTRACT`；该处是本项目缓存策略的权威说明。
- 允许根据功能需要新增或扩展提示词，但必须尽可能保持请求最长公共前缀固定，以提高供应商提示词缓存命中率。新增内容应先判断其稳定性：跨请求长期不变的规则可合并到既有稳定槽位；会随任务、项目状态或用户输入变化的内容必须放入动态尾部。
- 新增提示词时优先扩展既有固定槽位，不要轻易增加、删除或重排槽位。若确需改变稳定前缀结构，应集中完成相关改动，并同步补充或更新缓存契约测试。
- 请求消息必须保持“稳定内容在前、动态内容在后”：`buildStableSystemPrefix` 固定为 6 个 system 槽位，`buildDynamicTurnMessages` 固定为 8 个 system 槽位加 1 个 user 槽位。
- 不得在固定槽位之间插入可选消息。可选内容为空时必须保留既有占位文本，避免槽位索引或字节前缀发生变化。
- 稳定前缀仅放跨轮次、低频变化的内容：写作系统提示、执行规则、项目指令、技能目录、稳定风格约束，以及固定的模式附加占位。路径、章节正文、时间戳、当前任务、todos、用户选区、终审专则、范文正文及章节声线样本等必须放入动态尾部。
- 槽位 4/5 不得随 `intensive` 或 `taskMode`（含 audit）切换文案；否则 brainstorm→write→audit 会整段打穿供应商前缀缓存。
- 稳定提示词的文案调整会使供应商前缀缓存失效；非必要不要改动，相关调整应集中完成。
- 动态尾部应尽量短，优先传递摘要、ID 和路径；完整正文通过工具结果或分页读取获得，不要自动注入大段内容。
- 单次 `runAgent` 任务首次调用模型后，消息序列只能追加，不得修改已有消息，也不得在任务中途执行 compact、rehydrate、清除旧 reasoning 或压缩已完成工具调用等操作。此类整理仅可在活动任务之外重建会话时执行。
- `src/tools/schema.ts` 中的工具名称、顺序、描述和参数 schema 属于供应商缓存前缀。不得注入项目实时路径、角色 ID 或会话状态，不得按任务模式或项目动态切换工具集合。
- 工具 schema 发生结构性变化时，必须同步更新 `src/agent_cache.test.ts` 中的 `agentToolSchemaHash()` 期望值，并运行相关测试。
- `planWritingTask` 的固定规则放在 system 消息中；请求内容、目录和短历史只放在 user 消息中。
- 避免在 system、style、task workflow 和 review 中重复粘贴相同规则；句式硬约束以稳定「风格锚定」为准，工作流只用 `proseMannerismPreflightLine()` 交叉引用。
- 工具 schema 描述宜短：每次 step 都会完整发送 `TOOLS`；结构性变更后必须更新 `agentToolSchemaHash` 期望值。
- 角色扮演链路遵守 `src/roleplay.ts` 的独立固定 4 槽位前缀策略，不与主 Agent 的前缀布局混用。

## npm 与运行环境

- 整体修改完成之后才进行 `npm run build`，不要做非必要的测试；改完源码后须编译再交付/验收
- 项目采用 ESM（`"type": "module"`）。TypeScript 源码中的本地运行时导入继续使用 `.js` 后缀，并遵循现有模块风格。
- 排查会话/Job/提案时只读查询项目库：见 `docs/db-query.md`（`p/<name>/.writer/writer.db`）

## 修改原则

- 优先做最小、局部且可验证的修改，保持现有缓存命中策略、消息顺序和公开行为不变。
- 不提交 `dist/`、缓存目录、日志或其他生成物，除非当前任务明确要求。
- 不覆盖与当前任务无关的本地修改。

## 功能禁令

- 禁止视觉验收
- 禁止仅使用正则表达或者关键词来匹配用户语义，但是可以用于审查和审核输出内容
- 禁止进行复杂、带模拟数据和请求体的功能性测试，只测能不能用。测完后删除无效测试文件。
